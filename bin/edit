#!/bin/sh
# Opened from a terminal? Take it over. Otherwise spawn one.
if [ -t 0 ] && [ -t 1 ]; then
    exec nvim "$@"
elif [ -n "$WAYLAND_DISPLAY" ] || [ -n "$DISPLAY" ]; then
    exec st -e nvim "$@"
else
    exec nvim "$@"
fi
